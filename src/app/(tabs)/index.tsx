import { Screen } from '@/components/ui/screen';
import { Text } from '@/components/ui/text';

export default function MapScreen() {
  return (
    <Screen title="Map" subtitle="Live aircraft around you">
      <Text tone="muted">The live map arrives in a later phase.</Text>
    </Screen>
  );
}
