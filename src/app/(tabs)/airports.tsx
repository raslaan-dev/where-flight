import { Screen } from '@/components/ui/screen';
import { Text } from '@/components/ui/text';

export default function AirportsScreen() {
  return (
    <Screen title="Airports" subtitle="Arrivals and departures">
      <Text tone="muted">Airport schedules arrive in a later phase.</Text>
    </Screen>
  );
}
